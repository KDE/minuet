diff --git a/KF6KirigamiMacros.cmake b/KF6KirigamiMacros.cmake
index 5acd18af..ec8c4994 100644
--- a/KF6KirigamiMacros.cmake
+++ b/KF6KirigamiMacros.cmake
@@ -6,14 +6,15 @@
 include(ExternalProject)
 
 function(kirigami_package_breeze_icons)
+    set(_oneValueArgs TARGET)
     set(_multiValueArgs ICONS)
-    cmake_parse_arguments(ARG "" "" "${_multiValueArgs}" ${ARGN} )
+    cmake_parse_arguments(ARG "" "${_oneValueArgs}" "${_multiValueArgs}" ${ARGN} )
 
     if(NOT ARG_ICONS)
         message(FATAL_ERROR "No ICONS argument given to kirigami_package_breeze_icons")
     endif()
 
-    if(NOT ANDROID)
+    if(NOT ANDROID AND NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")
         return() # not needed on other platforms
     endif()
 
@@ -61,6 +62,47 @@ function(kirigami_package_breeze_icons)
         view-right-close
     )
 
+    if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
+        if(NOT ARG_TARGET)
+            message(FATAL_ERROR "No TARGET argument given to kirigami_package_breeze_icons on iOS")
+        endif()
+        if(NOT TARGET ${ARG_TARGET})
+            message(FATAL_ERROR "Target '${ARG_TARGET}' does not exist")
+        endif()
+
+        find_path(_BREEZE_THEME_DIR
+            NAMES index.theme
+            PATHS "${KDE_INSTALL_FULL_ICONDIR}/breeze"
+            NO_DEFAULT_PATH
+        )
+        if(NOT _BREEZE_THEME_DIR)
+            message(FATAL_ERROR "Could not find the installed Breeze icon theme")
+        endif()
+
+        set(_breeze_resource_files "${_BREEZE_THEME_DIR}/index.theme")
+        foreach(_iconName IN LISTS ARG_ICONS)
+            file(GLOB _icon_paths LIST_DIRECTORIES FALSE "${_BREEZE_THEME_DIR}/*/22/${_iconName}.svg")
+            list(LENGTH _icon_paths _count_paths)
+            if(_count_paths EQUAL 0)
+                message(WARNING "Could not find Breeze icon '${_iconName}'")
+                continue()
+            endif()
+            if(_count_paths GREATER 1)
+                message(WARNING "Found more than one version of '${_iconName}': ${_icon_paths}")
+            endif()
+            list(GET _icon_paths 0 _icon_path)
+            list(APPEND _breeze_resource_files "${_icon_path}")
+        endforeach()
+
+        list(REMOVE_DUPLICATES _breeze_resource_files)
+        qt_add_resources(${ARG_TARGET} ${ARG_TARGET}_kirigami_breeze_icons
+            PREFIX /icons/breeze
+            BASE "${_BREEZE_THEME_DIR}"
+            FILES ${_breeze_resource_files}
+        )
+        return()
+    endif()
+
     function(_find_breeze_icon icon)
         foreach(_size 48 32 22 16 12)
             SET(path "")
diff --git a/autotests/tst_globaldrawer.qml b/autotests/tst_globaldrawer.qml
index 380b67da..f32ce826 100644
--- a/autotests/tst_globaldrawer.qml
+++ b/autotests/tst_globaldrawer.qml
@@ -63,14 +63,16 @@ TestCase {
                 }
             ]
 
-            // Create some item which we can use to measure actual header height
-            Rectangle {
-                id: topItem
-                Layout.fillWidth: true
-                Layout.fillHeight: true
-                color: "green"
-                radius: 20 // to see its bounds
-            }
+            topContent: [
+                // Create some item which we can use to measure actual header height
+                Rectangle {
+                    id: topItem
+                    Layout.fillWidth: true
+                    implicitHeight: 60
+                    color: "green"
+                    radius: 20 // to see its bounds
+                }
+            ]
         }
 
         pageStack.initialPage: startPage
@@ -101,35 +103,59 @@ TestCase {
         }
         const app = createTemporaryObject(appItemComponent, this);
         verify(app);
-        const { headerItem, topItem } = app;
+        const { headerItem } = app;
+        app.globalDrawer.showHeaderWhenCollapsed = true;
 
         compare(app.globalDrawer.parent, app.T.Overlay.overlay);
 
         waitForRendering(app.globalDrawer.contentItem);
 
-        const overlay = T.Overlay.overlay;
         verify(headerItem.height !== 0);
 
-        // Due to margins, position won't be exactly zero...
-        const position = topItem.mapToItem(overlay, 0, 0);
-        verify(position.y > 0);
-        const oldY = position.y;
+        const drawerLayout = app.globalDrawer.contentItem;
+        const oldY = drawerLayout.contentItem.y;
+        verify(oldY > 0);
 
-        // ...but with visible header it would be greater than with invisible.
+        // With visible header the content item is lower than with invisible header.
         headerItem.visible = false;
         tryVerify(() => {
-            const position = topItem.mapToItem(overlay, 0, 0);
-            return position.y < oldY;
+            return drawerLayout.contentItem.y < oldY;
         });
 
         // And now return it back to where we started.
         headerItem.visible = true;
         tryVerify(() => {
-            const position = topItem.mapToItem(overlay, 0, 0);
-            return position.y === oldY;
+            return drawerLayout.contentItem.y === oldY;
         });
     }
 
+    function test_topPaddingAppliedOnce() {
+        const app = createTemporaryObject(appItemComponent, this);
+        verify(app);
+
+        const drawer = app.globalDrawer;
+        const topItem = app.topItem;
+        drawer.topPadding = 0;
+        waitForRendering(drawer.contentItem);
+
+        const initialY = topItem.mapToItem(T.Overlay.overlay, 0, 0).y;
+        const padding = 40;
+        drawer.topPadding = padding;
+
+        tryVerify(() => {
+            const paddedY = topItem.mapToItem(T.Overlay.overlay, 0, 0).y;
+            return Math.round(paddedY - initialY) === padding;
+        });
+    }
+
+    function test_topContentUsesImplicitHeight() {
+        const app = createTemporaryObject(appItemComponent, this);
+        verify(app);
+        waitForRendering(app.globalDrawer.contentItem);
+
+        compare(Math.round(app.topItem.parent.height), Math.round(app.topItem.parent.implicitHeight));
+    }
+
     component AppItemLoaderComponent : Kirigami.ApplicationItem {
         globalDrawer: globalDrawerLoader.item
         contextDrawer: contextDrawerLoader.item
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 5b523bf8..f15763c9 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -127,6 +127,13 @@ qt_target_qml_sources(Kirigami RESOURCES
     OUTPUT_TARGETS _out_targets_1
 )
 
+if (CMAKE_SYSTEM_NAME STREQUAL "iOS")
+    qt_target_qml_sources(Kirigami RESOURCES
+        styles/iOS/Theme.qml
+        OUTPUT_TARGETS _out_targets_ios
+    )
+endif()
+
 if (DESKTOP_ENABLED)
     qt_target_qml_sources(Kirigami RESOURCES
         styles/org.kde.desktop/AbstractApplicationHeader.qml
@@ -190,7 +197,7 @@ else()
     )
 endif()
 
-install(TARGETS Kirigami ${_out_targets_1} ${_out_targets_2} EXPORT KirigamiTargets ${KF_INSTALL_TARGETS_DEFAULT_ARGS})
+install(TARGETS Kirigami ${_out_targets_1} ${_out_targets_ios} ${_out_targets_2} EXPORT KirigamiTargets ${KF_INSTALL_TARGETS_DEFAULT_ARGS})
 install(EXPORT KirigamiTargets
     DESTINATION ${KDE_INSTALL_CMAKEPACKAGEDIR}/KF6Kirigami
     FILE KF6KirigamiTargets.cmake
diff --git a/src/controls/GlobalDrawer.qml b/src/controls/GlobalDrawer.qml
index 93ee232a..5cd73b13 100644
--- a/src/controls/GlobalDrawer.qml
+++ b/src/controls/GlobalDrawer.qml
@@ -585,9 +585,7 @@ KC.OverlayDrawer {
                         Layout.leftMargin: root.leftPadding
                         Layout.rightMargin: root.rightPadding
                         Layout.bottomMargin: Platform.Units.smallSpacing
-                        Layout.topMargin: root.topPadding
                         Layout.fillWidth: true
-                        Layout.fillHeight: true
                         Layout.preferredHeight: implicitHeight * opacity
                         // NOTE: why this? just Layout.fillWidth: true doesn't seem sufficient
                         // as items are added only after this column creation
diff --git a/src/controls/private/GlobalDrawerActionItem.qml b/src/controls/private/GlobalDrawerActionItem.qml
index 95d2db3b..7a8200b2 100644
--- a/src/controls/private/GlobalDrawerActionItem.qml
+++ b/src/controls/private/GlobalDrawerActionItem.qml
@@ -28,17 +28,20 @@ QQC2.ItemDelegate {
     readonly property bool hasChildren: kAction ? kAction.children.length > 0 : false
     readonly property bool hasVisibleMenu: actionsMenu?.visible ?? false
     readonly property bool hasToolTip: kAction ? kAction.tooltip !== "" : false
+    readonly property bool useIOSDarkDrawerStyle: Qt.platform.os === "ios" && Qt.styleHints.colorScheme === Qt.Dark
 
     checked: checkedBinding()
     highlighted: checked
     activeFocusOnTab: true
+    palette.light: Platform.Theme.backgroundColor
 
     contentItem: RowLayout {
         spacing: Platform.Units.largeSpacing
 
         Primitives.Icon {
             id: iconItem
-            color: listItem.tAction.icon.color
+            color: listItem.useIOSDarkDrawerStyle ? Platform.Theme.textColor : listItem.tAction.icon.color
+            isMask: listItem.useIOSDarkDrawerStyle
             source: listItem.tAction.icon.name || listItem.tAction.icon.source
 
             readonly property int size: Platform.Units.iconSizes.smallMedium
@@ -47,7 +50,7 @@ QQC2.ItemDelegate {
             Layout.minimumWidth: size
             Layout.maximumWidth: size
 
-            selected: (listItem.highlighted || listItem.checked || listItem.down)
+            selected: listItem.useIOSDarkDrawerStyle || listItem.highlighted || listItem.checked || listItem.down
             visible: source !== undefined && !listItem.isSeparator
         }
 
@@ -111,6 +114,22 @@ QQC2.ItemDelegate {
         }
     }
 
+    Binding {
+        property: "color"
+        restoreMode: Binding.RestoreBindingOrValue
+        target: listItem.background
+        value: Platform.Theme.backgroundColor
+        when: listItem.useIOSDarkDrawerStyle && listItem.background
+    }
+
+    Binding {
+        property: "visible"
+        restoreMode: Binding.RestoreBindingOrValue
+        target: listItem.background && listItem.background.children.length > 0 ? listItem.background.children[0] : null
+        value: false
+        when: listItem.useIOSDarkDrawerStyle && listItem.background && listItem.background.children.length > 0
+    }
+
     Accessible.name: Primitives.MnemonicData.plainTextLabel
     Primitives.MnemonicData.enabled: enabled && visible
     Primitives.MnemonicData.controlType: Primitives.MnemonicData.MenuItem
diff --git a/src/styles/iOS/Theme.qml b/src/styles/iOS/Theme.qml
new file mode 100644
index 00000000..e4c84cda
--- /dev/null
+++ b/src/styles/iOS/Theme.qml
@@ -0,0 +1,116 @@
+/*
+ *  SPDX-FileCopyrightText: 2026 Sandro Andrade <sandroandrade@kde.org>
+ *
+ *  SPDX-License-Identifier: LGPL-2.0-or-later
+ */
+
+import QtQuick
+import org.kde.kirigami as Kirigami
+
+Kirigami.BasicThemeDefinition {
+    readonly property bool darkMode: Qt.styleHints.colorScheme === Qt.Dark
+    readonly property color controlBackgroundColor: darkMode ? palette.window : palette.base
+    readonly property color controlTextColor: darkMode ? "white" : palette.windowText
+
+    textColor: controlTextColor
+    disabledTextColor: disabledPalette.windowText
+
+    highlightColor: palette.highlight
+    highlightedTextColor: darkMode ? "white" : palette.highlightedText
+    backgroundColor: controlBackgroundColor
+    alternateBackgroundColor: darkMode ? controlBackgroundColor : Qt.darker(controlBackgroundColor, 1.05)
+    activeTextColor: highlightedTextColor
+    activeBackgroundColor: palette.highlight
+    linkColor: "#2980B9"
+    linkBackgroundColor: "#2980B9"
+    visitedLinkColor: "#7F8C8D"
+    visitedLinkBackgroundColor: "#7F8C8D"
+    hoverColor: palette.highlight
+    focusColor: palette.highlight
+    negativeTextColor: "#DA4453"
+    negativeBackgroundColor: "#DA4453"
+    neutralTextColor: "#F67400"
+    neutralBackgroundColor: "#F67400"
+    positiveTextColor: "#27AE60"
+    positiveBackgroundColor: "#27AE60"
+
+    buttonTextColor: controlTextColor
+    buttonBackgroundColor: controlBackgroundColor
+    buttonAlternateBackgroundColor: alternateBackgroundColor
+    buttonHoverColor: palette.highlight
+    buttonFocusColor: palette.highlight
+
+    viewTextColor: controlTextColor
+    viewBackgroundColor: controlBackgroundColor
+    viewAlternateBackgroundColor: darkMode ? controlBackgroundColor : palette.alternateBase
+    viewHoverColor: palette.highlight
+    viewFocusColor: palette.highlight
+
+    selectionTextColor: highlightedTextColor
+    selectionBackgroundColor: palette.highlight
+    selectionAlternateBackgroundColor: Qt.darker(palette.highlight, 1.05)
+    selectionHoverColor: palette.highlight
+    selectionFocusColor: palette.highlight
+
+    tooltipTextColor: controlTextColor
+    tooltipBackgroundColor: controlBackgroundColor
+    tooltipAlternateBackgroundColor: alternateBackgroundColor
+    tooltipHoverColor: palette.highlight
+    tooltipFocusColor: palette.highlight
+
+    complementaryTextColor: controlTextColor
+    complementaryBackgroundColor: controlBackgroundColor
+    complementaryAlternateBackgroundColor: alternateBackgroundColor
+    complementaryHoverColor: palette.highlight
+    complementaryFocusColor: palette.highlight
+
+    headerTextColor: controlTextColor
+    headerBackgroundColor: controlBackgroundColor
+    headerAlternateBackgroundColor: alternateBackgroundColor
+    headerHoverColor: palette.highlight
+    headerFocusColor: palette.highlight
+
+    defaultFont: fontMetrics.font
+
+    property list<QtObject> children: [
+        TextMetrics {
+            id: fontMetrics
+        },
+        SystemPalette {
+            id: palette
+            colorGroup: SystemPalette.Active
+        },
+        SystemPalette {
+            id: disabledPalette
+            colorGroup: SystemPalette.Disabled
+        }
+    ]
+
+    onSync: object => {
+        if (object.palette === undefined) {
+            return;
+        }
+
+        object.palette.windowText = object.Kirigami.Theme.textColor;
+        object.palette.text = object.Kirigami.Theme.textColor;
+        object.palette.buttonText = object.Kirigami.Theme.textColor;
+        object.palette.window = object.Kirigami.Theme.backgroundColor;
+        object.palette.base = object.Kirigami.Theme.backgroundColor;
+        object.palette.button = object.Kirigami.Theme.backgroundColor;
+        object.palette.alternateBase = object.Kirigami.Theme.alternateBackgroundColor;
+        object.palette.highlight = object.Kirigami.Theme.highlightColor;
+        object.palette.highlightedText = object.Kirigami.Theme.highlightedTextColor;
+
+        if (darkMode) {
+            object.palette.light = object.Kirigami.Theme.backgroundColor;
+            object.palette.midlight = object.Kirigami.Theme.backgroundColor;
+        }
+    }
+
+    function __propagateColorSet(object, context) {}
+
+    function __propagateTextColor(object, color) {}
+    function __propagateBackgroundColor(object, color) {}
+    function __propagatePrimaryColor(object, color) {}
+    function __propagateAccentColor(object, color) {}
+}
